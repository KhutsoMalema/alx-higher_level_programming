#!/bin/bash

# Check if the environment variable $PYCODE is set
if [ -z "$PYCODE" ]; then
    echo "Error: Environment variable \$PYCODE is not set."
    exit 1
fi

# Run the Python code
python3 -c "$PYCODE"
